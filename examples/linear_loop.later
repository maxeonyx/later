# Linear value created in loop must be consumed each iteration
let mut count = 0
let mut i = 0
loop {
    if i >= 3 {
        break
    }
    let file = open("file.txt")
    file | close
    count = count + 1
    i = i + 1
}
count | to-string + " resources closed"
