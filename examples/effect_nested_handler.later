// Nested effect handlers - inner shadows outer
let my-effect = symbol()

handle {
    handle {
        send my-effect with "inner"
    } my-effect v {
        v + " handled"
    }
} my-effect v {
    v + " outer"
}
