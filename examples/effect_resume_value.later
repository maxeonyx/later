// Handler provides value when resuming

effect ask(question: String): String

fn converse() {
    let answer = ask("what is your name?")
    "Hello, {answer}!"
}

handle ask(q, resume) {
    print("Q: {q}")
    resume("World")
}
converse()
