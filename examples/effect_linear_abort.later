// Handler aborts: linear values must be cleaned up
let file = open("test.txt")
defer { 
    file | close
    print("cleanup ran")
}

handle {
    send abort with "reason"
    print("never reached")
} abort reason {
    print("aborted")
    // Handler does NOT continue - must ensure cleanup runs
}
