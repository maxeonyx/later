# Effect as capability - function must declare its effects

effect log(msg: String): ()

fn do-work() {  # implicitly has 'log' effect
    log("work started")
    log("work done")
}

handle log(msg) { print("[LOG] {msg}") }
do-work()
