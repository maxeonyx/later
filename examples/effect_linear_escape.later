// Error: linear value cannot escape through effect boundary

effect escape(value: File): ()

fn work() {
    let file = open("test.txt")
    escape(file)  // Error: linear value escapes!
}

handle escape(v) {
    v | close  // Handler can't receive linear value this way
}
work()
