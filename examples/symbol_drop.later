// Types can implement the built-in drop symbol for auto-cleanup
type Resource = {
    name: String,
    [drop]: fn(self) {
        print("dropped")
    }
}

let r = Resource { name: "test" }
// r goes out of scope, drop is called
