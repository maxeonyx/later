# Multiple effects compose

effect yield(value: Int): ()
effect log(msg: String): ()

fn work() {
    yield(1)
    log("step 1")
    yield(2)
    log("step 2")
}

handle yield(v) { print("yielded: {v}") }
handle log(msg) { print("logged: {msg}") }
work()
