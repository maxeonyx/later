// Deep handler handles all occurrences (default)

effect emit(value: Int): ()

fn work() {
    emit(1)
    emit(2)
    emit(3)
}

handle emit(v) { print("handled: {v}") }
work()
// Prints: handled: 1, handled: 2, handled: 3
